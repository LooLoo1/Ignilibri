#!/usr/bin/env node

console.log(process.argv)
console.log(process.cwd()) // That it!!!

//TODO - Later will be implement responsive `--help` base on size of terminal.
// console.log('Terminal size: ' + process.stdout.columns + 'x' + process.stdout.rows);